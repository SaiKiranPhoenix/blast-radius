#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Validate commit message against conventional commits spec
# Valid formats:
#   feat: add blast radius animation
#   fix(auth): handle missing token gracefully
#   chore!: drop Node 18 support (breaking change)
npx --no -- commitlint --edit "${1}"
