{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Production build: only compile src/, not tests or seed
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests", "seed"]
}
